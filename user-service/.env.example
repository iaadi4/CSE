## Server
APP_HOST=127.0.0.1
APP_PORT=8080
NODE_ENV=development

## Auth secrets
AUTH_SECRET=replace-with-strong-secret
AUTH_SECRET_EXPIRES_IN=15m
AUTH_REFRESH_SECRET=replace-with-strong-refresh-secret
AUTH_REFRESH_SECRET_EXPIRES_IN=1d

ADMIN_SECRET_KEY=

## Database (Neon Postgres)
# IMPORTANT: Do NOT include channel_binding in the connection string for Node/Prisma.
# Always require SSL. Use the pooled endpoint for serverless workloads, or direct endpoint otherwise.

# Example pooled endpoint (recommended):
# DATABASE_URL="postgresql://<user>:<password>@<endpoint>-pooler.<region>.aws.neon.tech/<db>?sslmode=require&pgbouncer=true&connect_timeout=30&pool_timeout=30&connection_limit=1"

# Example direct endpoint:
# DATABASE_URL="postgresql://<user>:<password>@<endpoint>.<region>.aws.neon.tech/<db>?sslmode=require&connect_timeout=30"

# Local development example:
# DATABASE_URL="postgresql://<username>:<password>@localhost:<port>/<database-name>?sslmode=disable"

# Optional Prisma tuning
# PRISMA_POOL_TIMEOUT=30